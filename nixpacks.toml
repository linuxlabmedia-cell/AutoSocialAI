[variables]
NODE_VERSION = "22"
NODE_ENV = "development"

[phases.install]
cmds = ["pnpm install"]

[phases.build]
cmds = [
  "pnpm --filter @autosocial/db exec prisma generate",
  "NODE_ENV=production pnpm --filter @autosocial/web build"
]

[start]
cmd = "pnpm --filter @autosocial/web start"
