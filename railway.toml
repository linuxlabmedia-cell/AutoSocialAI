[build]
builder = "nixpacks"
buildCommand = "pnpm install --force && pnpm --filter @autosocial/web build"

[deploy]
startCommand = "pnpm --filter @autosocial/web start"
healthcheckPath = "/"
healthcheckTimeout = 100
restartPolicyType = "on_failure"
